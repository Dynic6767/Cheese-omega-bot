Что было добавлено:

Создана отдельная таблица cheese_pairings для хранения сочетаний.

Добавлены описания сочетаний для большей информативности.

Реализована функция get_cheese_pairings() для удобного получения данных.

Сохранена целостность базы данных (нормализация).

В команду /start добавлена информация о новой команде /pairings.

В команду /help добавлена новая команда /pairings в список доступных команд.

Создан новый обработчик @bot.message_handler(commands=['pairings']) для команды /pairings, который:

вызывает функцию get_cheese_pairings() из модуля logic;

форматирует результат в читаемый вид — группирует сочетания по сырам, добавляет отступы и маркеры;

отправляет пользователю структурированный ответ;

обрабатывает случай, когда данных о сочетаниях нет.

В импорт из logic добавлена функция get_cheese_pairings.